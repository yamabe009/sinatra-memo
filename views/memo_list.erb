<div class="button-right">
  <a class="button" href="/memo/new">追加</a>
</div>
<ul>
  <% @memos.each do |memo| %>
    <li type="disc"><a href="/memo/<%= memo[:id] %>"><%= h(memo[:title]) %></a></li>
  <% end %>
</ul>
